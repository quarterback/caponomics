import { preset } from './_util'

// The invent-your-own starting point: no modules → cap sheet only, always legal.
// The entry point for any custom or minor league (A-League, CFL, a beer league)
// at any dollar scale.
export const blank = preset('blank', 'Blank — Invent Your Own', 'custom', [])
